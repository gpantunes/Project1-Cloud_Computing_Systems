<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://c-postgre-70231-70735.illbtqfddw4ggc.postgres.cosmos.azure.com:5432/citus?ssl=true&amp;sslmode=require</property>
        <property name="connection.username">citus</property>
        <property name="connection.password">S3cret1234</property>

        <!-- SSL settings for CosmosDB -->
        <property name="hibernate.connection.ssl">true</property>
        <property name="hibernate.connection.sslmode">require</property>

        <!-- JDBC connection pool settings -->
        <property name="connection.pool_size">64</property>
        
        <!-- Echo the SQL to stdout (optional, useful for debugging) -->
        <property name="show_sql">false</property>
        
        <!-- Define the Hibernate dialect for PostgreSQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</property>
        
        <!-- Set the current session context (recommended to use thread) -->
        <property name="current_session_context_class">thread</property>
        
        <!-- Define schema generation behavior (update, validate, create, etc.) -->
        <property name="hbm2ddl.auto">update</property>

        <!-- DBCP connection pool configuration (optional, can be tuned based on load) -->
        <property name="hibernate.dbcp.initialSize">5</property>
        <property name="hibernate.dbcp.maxTotal">20</property>
        <property name="hibernate.dbcp.maxIdle">10</property>
        <property name="hibernate.dbcp.minIdle">5</property>
        <property name="hibernate.dbcp.maxWaitMillis">-1</property>

        <!-- Class mappings (adjust these according to your entities) -->
        <mapping class="tukano.api.User" />
        <mapping class="tukano.api.Short" />
        <mapping class="tukano.impl.data.Following" />
        <mapping class="tukano.impl.data.Likes" />
    </session-factory>
</hibernate-configuration>
